apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: camel-quarkus-metrics
  labels:
    app: camel-quarkus
    openshift.io/user-monitoring: "true"
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: camel-quarkus
      app.kubernetes.io/component: management
  endpoints:
  - port: management
    path: /observe/metrics
    scheme: http
    interval: 30s